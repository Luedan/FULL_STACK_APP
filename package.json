{
  "name": "monorepo_ws",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "front:dev": "turbo front:dev --filter=front",
    "api:dev": "turbo api:dev --filter=api",
    "build": "turbo build",
    "prod": "turbo preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "pnpm": ">=6.0.0"
  },
  "packageManager": "pnpm@10.6.2",
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
